<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:CarsCollection" 
    x:Class="CarsCollection.CarsCollectionPage"
    Title="Car Collection">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Button Text="List Of Cars"
                  Command="{Binding NavToListCommand}"/>
            <Button Text="Create A Car"
                  Command="{Binding NavToCreateCommand}"/>
            <Button Text="Update a Car information"
                  Command="{Binding NavToUpdateCommand}"/>
            <Button Text="Delete a Car"
                  Command="{Binding NavToDeleteCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
